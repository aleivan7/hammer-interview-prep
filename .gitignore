# Local PHP SQLite modules used only when the system package is missing
.php-ext/

# Local Ralph worktrees and run logs
.ralph/

# Accidental root npm lockfile (real lockfile lives in frontend/)
/package-lock.json

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
